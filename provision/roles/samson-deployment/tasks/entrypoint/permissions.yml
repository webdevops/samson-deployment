---

- file:
    path:  /tmp
    owner: "root"
    group: "root"
    state: directory
    mode:  01777

- file:
    path:  "{{ item }}"
    owner: "{{ APPLICATION_USER }}"
    group: "{{ APPLICATION_GROUP }}"
    state: directory
    mode:  0755
  with_items:
    - /app/cached_repos
    - /app/db
